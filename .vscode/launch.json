// .vscode/launch.json
{
  "version": "0.2.0",
  "configurations": [
      {
          "type": "lldb",
          "request": "launch",
          "name": "Run your Executable",
          "program": "${workspaceFolder}/.build/debug/Run",
          "args": [],
          "cwd": "${workspaceFolder}",
          "preLaunchTask": "swift-build"
      }
  ]
}